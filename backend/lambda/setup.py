from setuptools import setup, find_packages

setup(
    name='lambda_return_data',
    version='0.1',
    description='Python library for lambda function that retrieve data from DB',
    author='L!ron Deri',
    packages=find_packages(),
)